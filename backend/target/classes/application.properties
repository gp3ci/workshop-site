# App Name
spring.application.name=demo

# STAGE 1: No database configuration is needed for the in-memory List API.
# The frontend can connect to the backend using the /api/posts endpoints.

# STAGE 2: Uncomment these settings after enabling JPA and H2 in pom.xml.
# Enable the H2 web console to view database tables in a browser.
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# H2 database connection settings.
# spring.datasource.url=jdbc:h2:mem:instalitedb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# Auto-generate SQL tables from Java @Entity classes.
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true